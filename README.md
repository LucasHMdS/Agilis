# Agilis
A cross-platform 2D game framework written in Swift, targeting Windows, Linux, and macOS.  Agilis provides a lightweight, backend-agnostic architecture with a built-in raylib backend for rendering, audio, and input. The core framework has zero platform-specific imports — all platform code lives behind swappable backend protocols.
